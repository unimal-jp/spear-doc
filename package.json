{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "private": true,
  "scripts": {
    "dev": "spear watch",
    "build": "yarn lint && spear build",
    "all": "cd ../ && yarn build && cd test && rm -rf node_modules && yarn && yarn build",
    "lint": "npx eslint ."
  },
  "dependencies": {
    "@mantaroh/spear-cli": "^1.9.10",
    "eslint": "^8.48.0",
    "front-matter": "^4.0.2",
    "node-html-parser": "^6.1.6",
    "remark": "^14.0.3",
    "remark-html": "^15.0.2"
  }
}
